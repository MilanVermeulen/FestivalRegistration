{% extends 'base.html.twig' %}

{% block title %}New Acts{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Create act</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_acts_index') }}">
                    View All Acts
                </a>
            </div>
            <div class="card-body">
                {{ include('acts/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
