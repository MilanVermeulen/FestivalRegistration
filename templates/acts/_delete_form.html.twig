<form method="post" action="{{ path('app_acts_delete', {'id': act.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ act.id) }}">
    <button class="btn">Delete</button>
</form>
